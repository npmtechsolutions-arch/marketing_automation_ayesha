# Runtime dependencies, pinned to the versions this project is developed and
# tested against. Development and test tooling lives in requirements-dev.txt.
#
# Pinned rather than ranged so a deploy installs what CI verified: an
# unpinned transitive bump is not something you want to discover in production.

# --- Web framework ---------------------------------------------------------
fastapi==0.137.1
uvicorn[standard]==0.52.4
python-multipart==0.0.32

# --- Data ------------------------------------------------------------------
SQLAlchemy==2.0.52
alembic==1.19.2
asyncpg==0.31.0

# --- Validation / settings -------------------------------------------------
pydantic==2.13.5
pydantic-settings==2.15.0
email-validator==2.3.0
python-dotenv==1.2.3

# --- Auth & crypto ---------------------------------------------------------
# PyJWT replaces python-jose, which is effectively unmaintained.
PyJWT[crypto]==2.13.0
# Used directly by app/core/crypto.py to encrypt stored platform credentials.
cryptography==50.0.1
passlib[bcrypt]==1.7.4
bcrypt==5.0.0
PyOTP==2.10.0
qrcode==8.2

# --- Background work & rate limiting ---------------------------------------
redis==6.4.0
slowapi==0.1.10

# --- Outbound HTTP ---------------------------------------------------------
# Runtime dependency, not just a test client: the Gemini calls, the SendGrid
# email transport and the OAuth flows all use it.
httpx==0.28.1

# --- Third-party services --------------------------------------------------
openai==3.8.0
stripe==15.6.1
boto3==1.43.89

# --- Media & templating ----------------------------------------------------
pillow==12.3.0
Jinja2==3.1.6
aiofiles==25.1.0

# --- Utilities -------------------------------------------------------------
python-dateutil==2.9.0.post0
tenacity==9.1.4
