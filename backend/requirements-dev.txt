# Development and test dependencies.
#
#     pip install -r requirements-dev.txt
#
# Includes the runtime requirements, so this is the only file a contributor
# needs to install.
-r requirements.txt

# --- Test runner -----------------------------------------------------------
pytest==9.1.1
pytest-asyncio==1.4.0

# --- Test database ---------------------------------------------------------
# The suite runs against in-memory SQLite, so it needs no Postgres or Redis.
# Test-only: production uses asyncpg.
aiosqlite==0.22.1

# --- Lint ------------------------------------------------------------------
ruff==0.16.6
